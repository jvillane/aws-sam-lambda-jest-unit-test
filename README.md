# aws-sam-lambda-jest-unit-test
A simple example of a lambda written using TypeScript + Node exposed by API Gateway and implemented using an AWS SAM template, inclusing unit tests using TypeScript + Jest
